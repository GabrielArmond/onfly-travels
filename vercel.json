{
  "builds": [
    {
      "src": "quasar.config.js",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/spa"
      }
    }
  ],
  "routes": [
    { "src": "/db/(.*)", "dest": "/public/db/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
